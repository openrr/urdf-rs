# https://docs.codecov.com/docs/codecov-yaml
coverage:
  status:
    # Only report coverage; do not fail CI based on coverage for now.
    project:
      default:
        informational: true
    patch:
      default:
        informational: true
comment: false
